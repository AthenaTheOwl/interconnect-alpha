# Spec 0002 tasks

- [x] Add no-argument `validate` CLI.
- [x] Check in canonical scenario metadata.
- [x] Check in fixture parquet inputs.
- [x] Check in readable report and JSONL report row.
- [x] Add tests for default validation.
- [ ] Replace fixture rows with public-source ingest outputs.
